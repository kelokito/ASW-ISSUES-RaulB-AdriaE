<%= stylesheet_link_tag "new_issue.css" %>

<!--<#%= form_with(model: issue) do |form| %>-->
<%= form_for @issue, :url => {:action => "bulkCreate"} do |form| %> <% if issue.errors.any? %>
<div style="color: red">
	<!-- <h2><#%= pluralize(issue.errors.count, "error") %> prohibited this issue from being saved:</h2> -->

	<ul>
		<% issue.errors.each do |error| %>
		<li><%= error.full_message %></li>
		<% end %>
	</ul>
</div>
<% end %>

<div style="margin-bottom: 0em;">
	<%= form.text_area :subject, class: 'new-issue-description', placeholder: "One item per line..."%>
</div>

<div>
	<%= form.button "Bulk create", class:"new-issue-submit" %>
</div>

<% end %>
