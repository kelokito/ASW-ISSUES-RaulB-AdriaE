<%= stylesheet_link_tag "new_issue.css" %>

<!--<#%= form_with(model: issue) do |form| %>-->
<%= form_for @issue, :url => {:action => "updateDueDate"} do |form| %> <% if issue.errors.any? %>
<div style="color: red">
	<!-- <h2><#%= pluralize(issue.errors.count, "error") %> prohibited this issue from being saved:</h2> -->

	<ul>
		<% issue.errors.each do |error| %>
		<li><%= error.full_message %></li>
		<% end %>
	</ul>
</div>
<% end %>

<div style="margin-bottom: 0em;">
	<%= form.date_field :due_date, class: "due-date no-focus"%>
</div>

<div>
	<%= form.button "Save", class:"new-issue-submit" %>
</div>

<% end %>
