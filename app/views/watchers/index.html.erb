<p style="color: green"><%= notice %></p>

<h1>Watchers</h1>

<div id="watchers">
  <% @watchers.each do |watcher| %>
    <%= render watcher %>
    <p>
      <%= link_to "Show this watcher", watcher %>
    </p>
  <% end %>
</div>

<%= link_to "New watcher", new_watcher_path %>
